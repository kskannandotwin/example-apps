import { ZippyToggleDirective } from './zippy-toggle.directive';

describe('ZippyToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ZippyToggleDirective();
    expect(directive).toBeTruthy();
  });
});
